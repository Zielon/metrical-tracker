# Tracker config

actor: './input/justin'
save_folder: './output/'
optimize_shape: true
optimize_jaw: true
begin_frames: 1
keyframes: [ 0, 1 ]
crop_image: false
image_size: [ 540, 960 ]  # height, width